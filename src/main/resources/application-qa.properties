# database connection
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DIARY_QA_HOST}:${DIARY_QA_PORT}/${DIARY_QA_DB}
spring.datasource.username=${DIARY_QA_USERNAME}
spring.datasource.password=${DIARY_QA_PASSWORD}

# JPA
spring.jpa.hibernate.ddl-auto=update